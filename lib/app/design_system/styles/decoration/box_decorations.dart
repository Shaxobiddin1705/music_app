import 'package:flutter/material.dart';

const BorderRadius defaultBorderRadius = BorderRadius.all(
  Radius.circular(16),
);

const BorderRadius borderRadius8 = BorderRadius.all(
  Radius.circular(8),
);

const BorderRadius borderRadius12 = BorderRadius.all(
  Radius.circular(12),
);

const RoundedRectangleBorder defaultRectangleBorder = RoundedRectangleBorder(
  borderRadius: defaultBorderRadius,
);
